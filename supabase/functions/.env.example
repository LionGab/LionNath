# =====================================================
# VARIÁVEIS DE AMBIENTE - Supabase Edge Functions
# =====================================================
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha os valores abaixo com suas credenciais
# 3. NUNCA commite o arquivo .env
#
# ⚠️ ATENÇÃO: Estas são chaves SENSÍVEIS que NUNCA devem
# ser expostas no client-side (mobile app).
# =====================================================

# =====================================================
# SUPABASE SERVICE ROLE KEY (OBRIGATÓRIO)
# =====================================================
# ⚠️ CRÍTICO: Esta chave tem acesso total ao banco.
# Use APENAS em Edge Functions, NUNCA no mobile app.
# Obtenha em: https://app.supabase.com/project/YOUR_PROJECT/settings/api

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# URL do Supabase (mesma do mobile, mas sem EXPO_PUBLIC_)
SUPABASE_URL=https://your-project-id.supabase.co

# =====================================================
# PROVEDORES DE IA (OBRIGATÓRIO para funcionalidades de IA)
# =====================================================
# 
# ⚠️ SEGURANÇA: Estas chaves NUNCA devem ser EXPO_PUBLIC_
# Elas devem estar APENAS aqui (Edge Functions) ou em GitHub Secrets

# Google Gemini 2.0 Flash (Chat NathIA)
GEMINI_API_KEY=your-gemini-api-key-here

# Anthropic Claude (Análise e curadoria)
CLAUDE_API_KEY=your-claude-api-key-here

# Perplexity AI (Curadoria de conteúdo)
PERPLEXITY_API_KEY=your-perplexity-api-key-here

# OpenAI (opcional, se usar GPT)
OPENAI_API_KEY=your-openai-api-key-here

# =====================================================
# CONFIGURAÇÕES DE IA
# =====================================================

# Provedor padrão para chat
LLM_PROVIDER=gemini

# Modelos específicos (opcional)
GEMINI_MODEL=gemini-2.0-flash-exp
CLAUDE_MODEL=claude-3-5-sonnet-20241022
PERPLEXITY_MODEL=llama-3.1-sonar-large-128k-online

# =====================================================
# RATE LIMITING
# =====================================================
# Limites de requisições por minuto

RATE_LIMIT_REQUESTS_PER_MINUTE=10
RATE_LIMIT_REQUESTS_PER_HOUR=100

# =====================================================
# SENTRY (OPCIONAL)
# =====================================================
# Para monitoramento de erros nas Edge Functions

SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ENVIRONMENT=production

# =====================================================
# AMBIENTE
# =====================================================

NODE_ENV=production

# =====================================================
# ⚠️ CHECKLIST DE SEGURANÇA
# =====================================================
# 
# Antes de fazer deploy:
# [ ] Todas as chaves de API estão aqui (não no mobile)
# [ ] Service Role Key está configurada
# [ ] .env está no .gitignore
# [ ] Variáveis estão configuradas no Supabase Dashboard > Edge Functions > Secrets
# [ ] Secrets estão configuradas no GitHub Actions (se usar CI/CD)
#
# =====================================================
